[package]
name = "equailizer-plugin"
edition = "2024"
version = "0.1.0"
description = "SDK for building equailizer plugins"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
